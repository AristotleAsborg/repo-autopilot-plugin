# 行尾与文本规范化（2026-09-12 加）。
#
# 为什么必须有这一份：本系统会**按文件内容的 git blob sha 判断"远端是不是已经一模一样"**
# （`tools/self_repo.py` 的幂等同步）。而在 Windows 上 git 默认 `core.autocrlf=true`，
# checkout 出来的是 CRLF、仓库里存的是 LF —— 两边算出来的 sha 不同，
# 于是"内容没变"被误判成"内容变了"：同步会反复重传，`test_blob_sha_matches_git_hash_object`
# 也会在 CI（windows-latest）上翻红。实测就是这么发现的（Linux/本机都不暴露）。
#
# 固定成 LF 之后，checkout 的字节 = 仓库里的字节 = 远端的字节，三处一致。
# `.cmd` 文件保持 CRLF（cmd 对行尾敏感，且我们历来要求它们 ASCII + CRLF 之外的写法都不碰）。
* text=auto eol=lf
*.cmd text eol=crlf
*.png binary
*.jpg binary
*.pdf binary
*.zip binary
