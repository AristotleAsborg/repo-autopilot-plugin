"""GitHub 封装 + 人类闸门（路线 1.4）。

安全红线在这里，不在提示词里：写操作只能作为 `perform` 回调传进
`approval.execute_if_approved()`，没有第二个入口。
"""

from .approval import (
    APPROVAL_WORD,
    APPROVALS_DIR,
    DEFAULT_TTL_HOURS,
    OUTBOX_DIR,
    REJECTION_WORD,
    STATUS_APPROVED,
    STATUS_EXPIRED,
    STATUS_PENDING,
    STATUS_REJECTED,
    WRITE_ACTIONS,
    ApprovalTicket,
    GateError,
    GateOutcome,
    approval_files,
    assert_write_action,
    decide,
    execute_if_approved,
    is_explicit_no,
    is_explicit_yes,
    queue_for_offline,
    request_approval,
    require_human_approval,
)
from .client import (
    BACKOFF_BASE,
    DEFAULT_BASE_URL,
    DEFAULT_TIMEOUT,
    MAX_ATTEMPTS,
    GitHubClient,
    GitHubError,
    Response,
    TransportError,
    safe_detail,
    with_backoff,
)
from .tokens import (
    READ_TOKEN_ENV,
    WRITE_TOKEN_ENV,
    load_write_token,
    looks_like_token,
    read_token,
    read_token_source,
    redact,
    scan_for_leaks,
    write_token_path,
    write_token_scope,
)

__all__ = [
    "APPROVALS_DIR",
    "APPROVAL_WORD",
    "BACKOFF_BASE",
    "DEFAULT_BASE_URL",
    "DEFAULT_TIMEOUT",
    "DEFAULT_TTL_HOURS",
    "MAX_ATTEMPTS",
    "OUTBOX_DIR",
    "READ_TOKEN_ENV",
    "REJECTION_WORD",
    "STATUS_APPROVED",
    "STATUS_EXPIRED",
    "STATUS_PENDING",
    "STATUS_REJECTED",
    "WRITE_ACTIONS",
    "WRITE_TOKEN_ENV",
    "ApprovalTicket",
    "GateError",
    "GateOutcome",
    "GitHubClient",
    "GitHubError",
    "Response",
    "TransportError",
    "approval_files",
    "assert_write_action",
    "decide",
    "execute_if_approved",
    "is_explicit_no",
    "is_explicit_yes",
    "load_write_token",
    "looks_like_token",
    "queue_for_offline",
    "read_token",
    "read_token_source",
    "redact",
    "request_approval",
    "require_human_approval",
    "safe_detail",
    "scan_for_leaks",
    "with_backoff",
    "write_token_path",
    "write_token_scope",
]
